import React from "react";

export default function ContactPage() {
  return (
    <section className="page-hero slim-hero contact-hold-page">
      <div className="container page-hero-inner article-width">
        <div className="eyebrow">Contact</div>
        <h1>Contact details are intentionally withheld pending approval.</h1>
        <p>
          The route remains active to preserve site structure. Direct details can be introduced once approved.
        </p>
      </div>
    </section>
  );
}
